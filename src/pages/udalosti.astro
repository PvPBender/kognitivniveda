---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Události | Společnost pro kognitivní vědu">
	<main>
		<h1>Naše <span class="text-gradient">události</span></h1>
		<div class="content">
			<div class="event">
				<div class="event-header">
					<h2>Spontánní komunikace prizmatem kognitivní lingvistiky</h2>
					<div class="event-meta">
						<span class="date">28. listopadu 2024</span>
						<span class="time">18:00</span>
						<span class="location">Kampus Hybernská</span>
					</div>
				</div>
				<div class="event-description">
					<p>Více informací bude doplněno...</p>
				</div>
			</div>
		</div>
	</main>
</Layout>

<style>
	main {
		margin: 6rem auto 2rem;
		padding: 1rem;
		width: 800px;
		max-width: calc(100% - 2rem);
		color: white;
		font-size: 20px;
		line-height: 1.6;
	}
	h1 {
		font-size: 3rem;
		font-weight: 700;
		line-height: 1;
		text-align: center;
		margin-bottom: 3rem;
	}
	.text-gradient {
		background-image: var(--accent-gradient);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 400%;
		background-position: 0%;
	}
	.content {
		background: rgba(var(--accent-dark), 0.2);
		border: 1px solid rgba(var(--accent-light), 0.2);
		border-radius: 12px;
		padding: 2rem;
	}
	.event {
		border-bottom: 1px solid rgba(var(--accent-light), 0.2);
		padding-bottom: 2rem;
		margin-bottom: 2rem;
	}
	.event:last-child {
		border-bottom: none;
		padding-bottom: 0;
		margin-bottom: 0;
	}
	.event-header {
		margin-bottom: 1.5rem;
	}
	h2 {
		color: rgb(var(--accent-light));
		font-size: 1.5rem;
		margin-bottom: 0.5rem;
	}
	.event-meta {
		display: flex;
		gap: 1.5rem;
		color: rgba(255, 255, 255, 0.8);
		font-size: 0.9em;
	}
	.date, .time, .location {
		display: flex;
		align-items: center;
	}
	.date::before {
		content: "📅";
		margin-right: 0.5rem;
	}
	.time::before {
		content: "⏰";
		margin-right: 0.5rem;
	}
	.location::before {
		content: "📍";
		margin-right: 0.5rem;
	}
	.event-description {
		color: rgba(255, 255, 255, 0.9);
	}
</style>